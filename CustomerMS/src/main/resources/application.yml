spring:
  application:
    name: CUSTOMERMS
server:
  port: 4451
eureka:
  client:
    service-url:
      defaultZone: http://localhost:4444/eureka/
    register-with-eureka: true
    fetch-registry: true